# makepkg scripts for sourcemod related stuff

Currently supported:
* ambuild
* spcomp

The PKGBUILD files are located in the corresponding subdirectories. Not that some packages depend on others (e.g. spcomp needs ambuild to compile).

